# The-Great-Station

The Great Station is a system built using HTML, CSS, JavaScript, JQuery, Moment.js and the Firebase Database to record users' inputs for their train, its destination, the frequency of when it travels and when the first departure time is in order to learn when it will arrive next. 
